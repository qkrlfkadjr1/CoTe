"""
소수를 먼저 다 구해놓고, 주어진 입력값을 소수 리스트에서 오름차순으로 나눌 수 있을만큼 나누려 함
소수를 먼저 다 구하기에는 범위가 너무 커서 시간이 초과됨

소수를 먼저 구해 놓을 필요가 없이, 주어진 입력값을 2부터 차례대로 나누다 보면 에라토스테네스의 체의 개념이 그대로 적용됨
(2로 먼저 나눌 수 있을 만큼 나누기 때문에 2의 배수(소수가 아닌 수)들은 알아서 제껴짐, 3, 5, 7등 도 마찬가지)

마지막으로, 나눌 수 있을때까지 나누고, 더이상 나누어지지 않으면 다음 수로 넘어간다는 개념은 매우 쉬우나 생각이 들지 않았음

a%i==0이면 나누고, 아니면 i+=1을 하면 됨
for i in range(a)...로 시작하는 습관을 좀 버려야 할듯

"""

a=int(input())
i=2
while a>1:
	if a%i==0:
		print(i)
		a//=i
	else:
		i+=1
