n=int(input())
a=list(map(int, input().split()))
sort_a=sorted(set(a))
dict_a={i:v for v, i in enumerate(sort_a)}
for i in a:
	print(dict_a[i], end=" ")
  
"""
시간복잡도를 처리해야 풀 수 있는 문제
문제의 해결 아이디어는 간단하다.
주어진 n만큼 수를 받아서, 각각의 수에 해당하는 인덱스 값을 구하여 차례대로 출력한다.
이 때, 문제가 되는 점은 인덱스(순서)를 구하는 과정이다.
리스트의 index함수를 이용하면 시간복잡도가 O(n^2)이 되어버려 가차없이 시간초과가 발생한다.
이 때, dict를 활용하여 문제를 해결할 수 있다.
dict의 경우 각각의 키(해당 문제에서의 각각의 수)를 입력 받고 해당 값(문제에서의 인덱스, 순서 값)을 찾아내는 시간 복잡도가 O(1)이므로
n개에 대하여 O(n)으로 매우 작아진다.
dict에 대해서는 아직 다루기 어색하므로 시간내어 공부해보자.
"""
