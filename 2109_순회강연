"""
생각해낸 알고리즘은, 입력받은 값들을 오름차순으로 정렬한 후에,
d일의 기한을 가진 값들은 큰 것부터 해서 d개까지만 남기고 나머지는 폐기한다.
그 후 살아남은 값들은 기한이 +1된다.
이를 반복하여서 마지막 값까지 처리했을 때 최종적으로 남은 값들의 합을 구한다.

이를 구현하는데에 있어서는 딕셔너리를 활용하는 것이 올바른 풀이법이라고 생각했다.
key에 기한을, value에 강연료를 넣어둔 후, key값에 해당하는 개수만 남기고 나머지는 다음 key값에 추가한다.

구현만 해보면 되지만, 연등 시간이 끝나가므로 이어서 풀겠음

참고 블로그:https://velog.io/@sezeom/%EC%88%9C%ED%9A%8C%EA%B0%95%EC%97%B0 (아직 참고는 안함, heapq가 아닌 딕셔너리 만으로도 풀어볼 예정임)
"""
