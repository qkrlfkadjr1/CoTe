문제 해결 방법은 다음과 같다.
1. 식량창고가 한 개 있을 때 최댓값은? d[0]=array[0]
2. 식량창고가 두 개 있을 때 최댓값은? d[1]=max(array[0], array[1])
3. 식량창고가 세 개 있을 때 최댓값은? d[2]=max(d[0]+array[2], d[1])
...
일반화. 식량창고가 i 개 있을 때 최댓값은? max(d[i-2]+array[i], d[i-1])
이때, i-3번째, i-4번째의 경우는 이미 앞에서 다 계산해서 최댓값을 만들어놓았기 때문에 고려할 필요가 없다.
